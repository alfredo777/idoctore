<%#= session[:user] = nil %>
<link href='http://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
<%= favicon_link_tag('favicon.ico')%>
<% if flash[:notice] %>
<div data-alert class="alert-box" id="alert_box_simple">
  <%= notice  %>
</div>
<% end %>
<script type="text/javascript">
	setTimeout(function(){
		$('#alert_box_simple').hide();
	    $('#alert_box_simple').html('');
	}, 3000);
</script>
<div class="fixed" style="margin-bottom:50px !important;">
      <nav class="top-bar">
      <center>
       <div class='large-10'>
        <ul class="title-area">
        <li class="name" style="margin-top: -8px;">
		  <a href="<%= root_path%>"  class="cursive" style="margin-top:-5px;"><dt><h2 style="color:#A4A4A4;" class="left cursive"> I </h2><h2 class="left cursive" style="color:#086A87;">Doctore</h2></dt></a>
		</li>
		</ul>
	    <section class="top-bar-section">

		  <% if session_status == false %>
		   
		   <% if session[:admin] != nil %>
		    <ul class="right">
			  <li><%= link_to 'Salir', "#{admin_end_session_path}" %></li>
			</ul>
		    <%else%>
		   <ul class="right nav-doctor">
			  <li><a href="#" id="whoare" data-target="whoare_a" ><%= t('menu.who_we_are')%></a></li>
			  <li><a href="#" id="whodo" data-target="what_we_do_a" ><%= t('menu.we_do')%></a></li>
			  <li><a href="#" id="banefits" data-target="banefits_a" ><%= t('menu.benefits')%></a></li>
			  <li><a href="#" id="price"  data-target="price_a" ><%= t('menu.prices')%></a></li>
			  <li><a href="#" id="price"  data-reveal-id="loggin_modal" ><%= t('menu.login')%></a></li>
		   </ul>

		   <%end%>
		  <%else%>
		   <ul class="right">
		     <li><%= link_to  t('tabs_from_menu_user.patients'), users_path if current_user.role == 'doctor' %><%= link_to  t('tabs_from_menu_user.doctors'), users_path if current_user.role == 'patient' %></li>
		     <li><%= link_to  "#{current_user.name}", user_path(current_user.id) %></li>
		     <% if current_user.role == 'doctor' %>
		     <li><%= link_to image_tag('payment.png', width:30, height:30), payments_path%> </li>
		     <%end%>
		     <li><%= link_to  image_tag('exit.png', width:30, height:30), sign_out_path(:id => current_user.id) %></li>
		    </ul>
		  <%end%>
		</section>
		</div>
		</center>
	    </nav>
</div>
  <% if current_user %>
  <div id="chat_notice_<%=current_user.id%>" style="margin-top:100px; position:fixed; height:auto; margin-left:50px; display:none; z-index:99999;" class="large-3">
  </div>
  <%end%>


