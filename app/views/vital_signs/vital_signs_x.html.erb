<% if  session[:assistant] == nil %>
<% if !agent %>
<div class="menu-select">
  <p style="font-size:30px; margin-top:20px;"><%= t('forms_tabs.vital_signs')%> - <%= link_to "#{@user.name}", user_path(@user.id) %>
  </p>
</div>
<%else%>
<div class="mobile_head">
  <%= t('forms_tabs.vital_signs')%>
</div>
<%end%>
<%else%>
<div class="act_head_lef_assistent">
  <%= t('forms_tabs.vital_signs')%> - <%= "#{@user.name}" %>
</div>
<%end%>

<div class="margin-regular">
  <center>
  <table>
    <thead>
      <tr>
        <th width="300"><%= t('vital_signs.vital_signs')%></th>
        <!--<th width="300">Médico</th>-->
      </tr>
    </thead>
    <tbody>
      <% @user_vital_signs.reverse.each do |vs| %>
      <%= render partial: '/vital_signs/show', locals: {vs: vs}%>
      <%end%>
    </tbody>
  </table>
  <ul class="pagination">
    <%= will_paginate @user_vital_signs %>
  </ul>
</div>
</center>