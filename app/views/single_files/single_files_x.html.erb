<div class="menu-select">
  <p style="font-size:30px; margin-top:20px;"><%= t('forms_tabs.analysis')%> - <%= link_to "#{@user.name}", user_path(@user.id)%>
  </p>
</div><div class=" margin-regular">
<center>

<table>
  <thead>
    <tr>
    <th width="300">Análisis Clínicos</th>
    </tr>
  </thead>
   <tbody>
   <% @single_files.reverse.each do |an| %>
        <%= render partial: '/single_files/show', locals: {an: an}%>
   <%end%>
  </tbody>
</table>
<ul class="pagination">
  <%= will_paginate @single_files %>
</ul>
</center>
</div>