<% if !agent %>
<div class="menu-select">
  <p style="font-size:30px; margin-top:20px;">Nuevo registro dental -  <%= link_to "#{@user.name}", user_path(@user.id) %>
  </p>
</div>
<%else%>
<div class="mobile_head">
  Nuevo registro dental 
</div>
<%end%>
<div class="row" style="background:#fff; padding:40px; margin-top:-20px;">
<%= render 'form' %>
</div>
